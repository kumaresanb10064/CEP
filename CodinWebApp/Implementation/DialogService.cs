using CodinWebApp.Interface;
using Microsoft.JSInterop;

namespace CodinWebApp.Implementation
{
    public class DialogService : IDialogService
    {
        //public async Task<bool> ShowMessageBox(string title, string message, string yesText, string noText)
        //{
        //    // Implement the dialog logic here
        //    // You can use browser's built-in JavaScript functions or a custom UI library
        //    // For example, you can use window.confirm
        //    bool confirmed = await JSRuntime.InvokeAsync<bool>("confirm", $"{title}\n\n{message}");
        //    return confirmed;
        //}
    }
}
